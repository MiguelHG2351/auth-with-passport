<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Error de session</title>
    <style>
      body {
        height: 100vh;
        width: 100vw;
        margin: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      }

      h1 {
        font-size: 12px;
        max-width: 60ch;
      }

      .links {
        margin-top: 2rem;
      }

      a {
        display: inline-block;
        text-decoration: none;
      }

      a:nth-of-type(1) {
        background: black;
        color: white;
        padding: .5rem 1rem;
        border: 1px solid #000;
        transition: background .10s ease-in;
      }

      a:nth-of-type(1):hover {
        color: black;
        background: white;
      }

      a:nth-of-type(2) {
        background: white;
        color: black;
        padding: .5rem 1rem;
        border: 1px solid #000;
        transition: background .10s ease-in;
      }

      a:nth-of-type(2):hover {
        opacity: .5;
        border: 1px solid rgba(0, 0, 0, 0.5);
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>
        Are you sure you want to log in again on this device? Please note that
        you already have an active session, and if you log in again, the current
        session will be invalidated.
      </h1>
      <div class="links">
        <a href="/">Back to home</a>
        <a href="/auth/logout">Continue</a>
      </div>
    </div>
  </body>
</html>
